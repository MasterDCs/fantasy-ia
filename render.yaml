services:
  - type: web
    name: fantasy-ia
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FANTASY_EMAIL
        sync: false
      - key: FANTASY_PASSWORD
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.9
